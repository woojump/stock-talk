[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.109.0",
    "httpx>=0.27.0",
    "pydantic-settings>=2.6.1",  # 따옴표 안에 등호를 넣고 형식을 맞춤
    "python-dotenv==1.0.0",
    "rich>=13.9.4",              # fastmcp 요구사항에 맞춰 버전 상향
    "uvicorn==0.27.0",
    "fastmcp",                   # 새로 추가할 패키지
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-core>=0.3.0",
    "langchain-community>=0.3.0",
    "langgraph>=1.0.6",
    "newspaper3k>=0.2.8",
    "lxml-html-clean>=0.4.3",
    "thefuzz>=0.22.1",
]
