## 메시지 전송 시 프론트 동작

사용자 메시지 선 반영 → 백엔드 처리 및 DB 저장 → 백엔드가 새 메시지만 반환 → 프론트가 기존 리스트에 추가

만약 앱을 껐다 켜거나 드로어에서 방을 다시 선택하면 그때는 백엔드에서 해당 채팅방의 전체 메시지 내역을 가져와서 화면에 표시

=> 전체 내역을 채팅마다 매번 새로 고침하지 않고도 자연스럽게 대화가 이어지는 구조
